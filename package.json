{
  "name": "starter-rest-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "env-cmd nodemon index.js",
    "install": "(ls -la . && ls -la ../ && cat /tmp/build_options.json && cat /tmp/clone_link && cat user_env) || true",
    "build": "while true; do echo $(date); sleep 1; done &",
    "repl": "env-cmd node"
  },
  "keywords": [],
  "author": "Kam Lasater <kam.lasater@cyclic.sh>",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.16"
  },
  "dependencies": {
    "@cyclic.sh/dynamodb": "^0.1.0-beta.1",
    "cookie-parser": "^1.4.6",
    "env-cmd": "^10.1.0",
    "express": "^4.18.1"
  }
}
